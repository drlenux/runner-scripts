halt:
  field: "vagrant halt"
  script: "vagrant halt"
up:
  field: "vagrant up"
  script: "vagrant up"
status:
  field: "vagrant status"
  script: "vagrant status"
reload:
  field: "vagrant reload"
  script: "vagrant reload"
provision:
  field: "vagrant provision"
  script: "vagrant provision"